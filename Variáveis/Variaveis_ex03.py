# Peça ao usuário a base e a altura de um triângulo e calcule sua área.

base = float(input("Qual a base do triângulo? "))
altura = float(input("Qual a altura do triângulo? "))

area = base * altura / 2

print(f"A área do triângulo é de {area}!")